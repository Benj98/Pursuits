import { userRepository } from "../database/repositories/user.repository"

// mp.events.add('playerQuit', async (player) => {
//     if(player.getVariable('loggedIn') == true) {
//         const user = await userRepository.findByUsername(player.name);
//     }
// })